baseURL = 'https://motleyplay.com/'
languageCode = 'en-us'
title = 'Motley Play'

# set `baseurl` to your root domain
# if you set it to "/" share icons won't work properly on production
copyright = "Motley Play"
# canonifyurls = true

theme = "hugo-clarity"
disqusShortname = "motleyplay"

DefaultContentLanguage = "en"
# [languages]
# config/_default/languages.toml

# [menus]
# config/_default/menus/menu.en.toml

[taxonomies]
category = "categories"
tag = "tags"
series = "series"

[outputs]
home = ["HTML", "RSS", "JSON"]

[pagination]
pagerSize = 10
